export type Room = {
  id: string;
  name: string;
  street: string;
  zipCode: string;
  city: string;
  description?: string;
  imageUrl?: string;
  capacity: number;
  locationId?: string;
  createdAt: string;
  updatedAt: string;
};
