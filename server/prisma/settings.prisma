model Settings {
  colTitles           Json?
  holidays            Json?
  schoolHolidays      Json?
  rebates             Json?
  voucher             Json?
  calendarPast        Boolean  @default(false)
  calendarOccurrences Int      @default(0)
  calendarLength      Int      @default(12)
  formFields          Json?
  domain              String?
  federalState        String?
  legalResources      String   @default("fileadmin/kunden/mandant/rechtstexte/")
  contracts           String[] @default([])
  registration        Json?
  calendarConfig      Json?    @default("{\"startHour\": 10, \"endHour\": 20, \"slotHeight\": 20, \"minutesPerSlot\": 15}")

  // Common fields
  id        String   @id @default(uuid()) @db.Uuid
  tenantId  String   @unique
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
